version: "3.9"

services:
  app:
    build: .
    container_name: business-number-parser
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=production
    volumes:
      - ./data:/app/data
    restart: unless-stopped

